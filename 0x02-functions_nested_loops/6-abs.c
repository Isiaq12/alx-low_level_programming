#include"main.h"
/**
 * _abs - compute absolute value of an integer
 *
 * @int: any value
 *
 * Return: absolutely
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
