#include"main.h"
/**
 * _isdigit - checks for digit only
 *
 * @c: character to print
 *
 * Return: Success
 */
int _isdigit(int c)
{
	if (c <= 0 || c >= 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
