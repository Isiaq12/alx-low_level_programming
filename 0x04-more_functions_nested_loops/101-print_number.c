#include"main.h"
/**
 * print_number - to display any digit
 *
 * @n: name of the variable
 *
 * Return: Success
 */
void print_number(int n)
{
	if (n < 0 || n > 0)
	{
		return (n);
	}
	else
	{
		return(0);
	}
}
