#include"main.h"

/**
 * print_rev - to print in a reverse order
 *
 * @s: string to be printed
 *
 * Return: Success
 */
void print_rev(char *s)
{
	if (*s != '\0')
	{
		print_rev(s + 1);
		_putchar(*s);
	}
	else
	{
		_putchar(*s);
	}
	_putchar('\n');
}
